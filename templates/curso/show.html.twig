{% extends 'base.html.twig' %}

{% block title %}Curso{% endblock %}

{% block body %}
    <h1>Curso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ curso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ curso.nombre }}</td>
            </tr>
            <tr>
                <th>Duracion</th>
                <td>{{ curso.duracion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('curso_index') }}">back to list</a>

    <a href="{{ path('curso_edit', {'id': curso.id}) }}">edit</a>

    {{ include('curso/_delete_form.html.twig') }}
{% endblock %}
